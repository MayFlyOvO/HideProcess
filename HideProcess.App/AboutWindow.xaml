<Window x:Class="HideProcess.App.AboutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Width="460"
        Height="320"
        MinWidth="440"
        MinHeight="300"
        WindowStyle="None"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        Icon="pack://application:,,,/Assets/HideProcess.ico"
        Background="#FFF6F8FB"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True"
        FontFamily="Segoe UI Variable Text">
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="0"
                            ResizeBorderThickness="0"
                            CornerRadius="14"
                            GlassFrameThickness="0"
                            UseAeroCaptionButtons="False" />
    </shell:WindowChrome.WindowChrome>

    <Window.Resources>
        <Style x:Key="TitleBarButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="34" />
            <Setter Property="Height" Value="30" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="Background" Value="#00FFFFFF" />
            <Setter Property="BorderBrush" Value="#00FFFFFF" />
            <Setter Property="Foreground" Value="#4B5563" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Root"
                                CornerRadius="8"
                                Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="#FFEFF2F6" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Background="#FFF6F8FB">
        <Border Margin="0"
                CornerRadius="14"
                Background="White"
                BorderThickness="0"
                BorderBrush="#00FFFFFF"
                ClipToBounds="True">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="46" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <Grid Grid.Row="0"
                      Background="#FFF7F8FA"
                      MouseLeftButtonDown="TitleBar_OnMouseLeftButtonDown">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <StackPanel Orientation="Horizontal"
                                VerticalAlignment="Center"
                                Margin="14,0,0,0">
                        <Image Width="24"
                               Height="24"
                               Source="pack://application:,,,/Assets/HideProcess.ico"
                               VerticalAlignment="Center"
                               SnapsToDevicePixels="True" />
                        <TextBlock x:Name="HeaderTextBlock"
                                   Margin="8,0,0,0"
                                   FontSize="13"
                                   FontWeight="SemiBold"
                                   Foreground="#1F2937"
                                   VerticalAlignment="Center" />
                    </StackPanel>

                    <Button Grid.Column="1"
                            Style="{StaticResource TitleBarButtonStyle}"
                            Margin="0,0,8,0"
                            Click="CloseButton_OnClick">
                        <TextBlock Text="&#xE8BB;"
                                   FontFamily="Segoe Fluent Icons"
                                   FontSize="10" />
                    </Button>
                </Grid>

                <Border Grid.Row="1"
                        Margin="16"
                        Padding="16"
                        CornerRadius="12"
                        Background="#FFFAFBFD"
                        BorderThickness="1"
                        BorderBrush="#FFE9EDF3">
                    <StackPanel>
                        <TextBlock x:Name="DescriptionTextBlock"
                                   FontSize="13"
                                   Foreground="#4A5560" />

                        <Grid Margin="0,14,0,0">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="120" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                            <TextBlock x:Name="VersionLabelTextBlock"
                                       FontSize="13"
                                       Foreground="#4A5560"
                                       VerticalAlignment="Center" />
                            <TextBlock x:Name="VersionValueTextBlock"
                                       Grid.Column="1"
                                       FontSize="13"
                                       FontWeight="SemiBold"
                                       Foreground="#1F2937"
                                       TextWrapping="Wrap"
                                       VerticalAlignment="Center" />

                            <TextBlock x:Name="PackageLabelTextBlock"
                                       Grid.Row="1"
                                       Margin="0,10,0,0"
                                       FontSize="13"
                                       Foreground="#4A5560"
                                       VerticalAlignment="Center" />
                            <TextBlock x:Name="PackageValueTextBlock"
                                       Grid.Row="1"
                                       Grid.Column="1"
                                       Margin="0,10,0,0"
                                       FontSize="13"
                                       FontWeight="SemiBold"
                                       Foreground="#1F2937"
                                       TextWrapping="Wrap"
                                       VerticalAlignment="Center" />

                            <TextBlock x:Name="AuthorLabelTextBlock"
                                       Grid.Row="2"
                                       Margin="0,10,0,0"
                                       FontSize="13"
                                       Foreground="#4A5560"
                                       VerticalAlignment="Center" />
                            <TextBlock x:Name="AuthorValueTextBlock"
                                       Grid.Row="2"
                                       Grid.Column="1"
                                       Margin="0,10,0,0"
                                       FontSize="13"
                                       FontWeight="SemiBold"
                                       Foreground="#1F2937"
                                       VerticalAlignment="Center" />

                            <TextBlock x:Name="GithubLabelTextBlock"
                                       Grid.Row="3"
                                       Margin="0,10,0,0"
                                       FontSize="13"
                                       Foreground="#4A5560"
                                       VerticalAlignment="Center" />
                            <TextBlock Grid.Row="3"
                                       Grid.Column="1"
                                       Margin="0,10,0,0"
                                       FontSize="13"
                                       VerticalAlignment="Center">
                                <Hyperlink x:Name="GithubLink"
                                           RequestNavigate="GithubLink_OnRequestNavigate">
                                    <Run x:Name="GithubLinkRun" />
                                </Hyperlink>
                            </TextBlock>
                        </Grid>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>
    </Grid>
</Window>
